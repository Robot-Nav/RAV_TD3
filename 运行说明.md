# limoRL 使用说明

## 快速开始

### 训练

```bash
source devel/setup.bash
roslaunch limo_gazebo_sim limoEnv.launch
source devel/setup.bash
roslaunch limoRL limo_TD3_train.launch
```

### 测试

```bash
source devel/setup.bash
roslaunch limo_gazebo_sim limoEnv.launch
source devel/setup.bash
roslaunch limoRL limo_TD3_test.launch episode:=-1 test_episodes:=20 max_steps:=1200
```


## 参数表

### 测试参数

| 参数 | 默认值 | 作用 | 修改建议 |
| --- | --- | --- | --- |
| `episode` | `-1` | 选择测试时加载的模型轮次。`-1` 表示自动选择最新完整 checkpoint。 | 可以手动指定，如 `episode:=1720`。 |
| `test_episodes` | `20` | 连续测试的回合数。 | 快速检查可设为 `5` 或 `10`；统计效果可设更大。 |
| `max_steps` | `1200` | 单个 episode 最大步数。 | 如果经常还没到目标就结束，可以调大。 |

### 训练参数

| 参数 | 默认值 | 作用 | 修改建议 |
| --- | --- | --- | --- |
| `random_goal_on_reset` | `true` | 是否在每次 reset 后刷新目标点。 | 建议保持 `true`。 |
| `goal_reset_interval` | `1` | 每隔多少个回合刷新一次目标点。 | `1` 表示每回合都换目标。 |
| `goal_clearance` | `0.20` | 目标点与障碍物的最小净空。 | 想更保守可调大；想增加可选点可调小。 |
| `goal_min_clearance` | `0.05` | 目标点的最低净空下限。 | 用于避免过度保守。 |
| `random_candidate_samples` | `140` | 每次刷新额外随机采样的候选点数量。 | 越大越分散，但计算略多。 |
| `min_goal_switch_distance` | `0.9` | 新旧目标之间最小切换距离。 | 用于避免目标在附近几个点之间反复跳。 |
| `replay_batch_size` | `128` | 训练时从经验回放池抽取的样本数。 | 显存不够可继续减小到 `64`。 |
| `cuda_empty_cache_interval` | `400` | 每隔多少次更新清理一次 CUDA 缓存。 | 显存持续上涨时可调小。 |
| `action_v_max` | `0.8` | 线速度上限。 | 机器人太慢可调大，太激进可调小。 |
| `action_w_max` | `1.8` | 角速度上限。 | 同上。 |
| `random_action_steps` | `1500` | 训练初期完全随机探索的步数。 | 想更早进入学习可调小。 |
| `max_steps` | `1000` | 单个训练 episode 的最大步数。 | 训练时间过长可适当调小。 |

### pth 选择说明

训练结果保存在：

```bash
src/limoRL/train/TD3/model/
```

测试时如果要手动选择模型，可以通过 `episode` 指定对应轮次，例如：

```bash
episode:=20
episode:=120
episode:=1720
```

建议优先选择完整的一组 checkpoint，也就是同一轮次下以下文件都存在：

- `TD3_actor_XXX.pth`
- `TD3_target_actor_XXX.pth`
- `TD3_critic1_XXX.pth`
- `TD3_target_critic1_XXX.pth`
- `TD3_critic2_XXX.pth`
- `TD3_target_critic2_XXX.pth`

如果只缺一部分，测试脚本会尽量回退到最新完整轮次。

## 常见问题

### 1. goal_box 偶尔看不到，为什么？
这是 Gazebo 的可视化刷新问题。目标模型在 Gazebo 中实际上还在，机器人也能继续导航到它，但视觉上可能会短暂消失。

### 2. 为什么目标点总在少数几个点之间切换？
通常是因为目标筛选过于保守，导致安全候选太少。现在已经加入随机候选池和更合理的净空参数，目的是让目标分布更自然。

### 3. 训练时显存不够怎么办？
优先调小 `replay_batch_size`，例如改成 `64`。如果训练很久后显存缓慢上涨，可以把 `cuda_empty_cache_interval` 调小一些。

### 4. 如果想让机器人更稳，应该改什么？
优先调 `action_v_max` 和 `action_w_max`。如果机器人过猛容易撞，就降低它们；如果太慢，可以适当提高。

### 5. 如果只想快速测试模型，怎么改参数？
可以优先修改：
- `episode`
- `test_episodes`
- `max_steps`

例如快速测试：

```bash
roslaunch limoRL limo_TD3_test.launch stage:=3 episode:=-1 test_episodes:=5 max_steps:=800
```

## 注意

- `episode:=-1` 表示自动选择最新完整 checkpoint。
- 如果重新打开 Gazebo 后 goal_box 仍偶尔不可见，属于bug。
